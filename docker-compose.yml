services:
  postgrtes:
    image: postgres:latest
    environment:
      POSTGRES_PASSWORD: 123
    ports:
      - "5432:5432"
    volumes:
      - ./src_data:/src_data
      - ./scripts:/docker-entrypoint-initdb.d/